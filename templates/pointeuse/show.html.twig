{% extends 'base.html.twig' %}

{% block title %}Pointeuse{% endblock %}

{% block body %}
    <h1>Pointeuse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pointeuse.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ pointeuse.date ? pointeuse.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ pointeuse.duree }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pointeuse_index') }}">back to list</a>

    <a href="{{ path('pointeuse_edit', {'id': pointeuse.id}) }}">edit</a>

    {{ include('pointeuse/_delete_form.html.twig') }}
{% endblock %}
